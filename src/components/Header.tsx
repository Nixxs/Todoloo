import { Typography } from "@mui/material";

const Header = () => {
	return (
		<Typography
			sx={{
				fontWeight: 600,
				fontSize: 26,
				padding: 1
			}}
		>
			ToDoLoo
		</Typography>
	);
}

export default Header
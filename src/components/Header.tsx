import { Typography } from "@mui/material";

const Header = () => {
	return (
		<Typography
			sx={{
				fontWeight: 600,
				fontSize: 20
			}}
		>
			ToDoLoo
		</Typography>
	);
}

export default Header